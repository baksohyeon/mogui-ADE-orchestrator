# The same three gates that run locally before a push, run where the author's
# word is not the evidence. The 0.1.0 changelog said it plainly: "a passing
# count in a pull request is the author's word." This closes that.
#
# What CI cannot cover, stated rather than implied: the organization rules file
# (REDACTION_EXTRA_PATTERNS) is deliberately not in the repository, so the scan
# here runs the committed ruleset only. The full scan with organization rules
# still runs locally before a push.
#
# gitleaks is installed in both jobs, not only the redaction one: the test
# suite exercises redaction-scan.sh for real (measured on the first run of this
# workflow — four commit message scan tests failed on a runner without it).
# The install block is duplicated across jobs because Actions YAML has no
# anchors across jobs and a composite action is more machinery than two copies.
name: gates

on:
  pull_request:
  push:
    branches: [main]

jobs:
  tests:
    # Windows is measurement-only for now; promote to blocking after three
    # consecutive green runs on main. The leg measures the Windows-compatible
    # subset; tests that need the Unix script execution surface (extensionless
    # shebangs / bash+WSL) are skipped via tests/windows_exec_surface.py and
    # remain the Windows work backlog.
    continue-on-error: ${{ matrix.measurement_only }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        include:
          - os: ubuntu-latest
            measurement_only: false
          - os: macos-latest
            measurement_only: false
          - os: windows-latest
            measurement_only: true
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: actions/setup-python@v7.0.0
        with:
          python-version: "3.12"
      - name: Install gitleaks (pinned, checksum verified)
        shell: bash
        run: |
          case "$RUNNER_OS" in
            Linux) f=gitleaks_8.30.1_linux_x64.tar.gz; sum=551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb ;;
            macOS) f=gitleaks_8.30.1_darwin_arm64.tar.gz; sum=b40ab0ae55c505963e365f271a8d3846efbc170aa17f2607f13df610a9aeb6a5 ;;
            Windows) f=gitleaks_8.30.1_windows_x64.zip; sum=d29144deff3a68aa93ced33dddf84b7fdc26070add4aa0f4513094c8332afc4e ;;
            *) echo "unmapped runner: $RUNNER_OS"; exit 1 ;;
          esac
          curl -sSfL -o gitleaks.pkg "https://github.com/gitleaks/gitleaks/releases/download/v8.30.1/$f"
          if command -v shasum >/dev/null 2>&1; then
            echo "$sum  gitleaks.pkg" | shasum -a 256 -c -
          else
            echo "$sum  gitleaks.pkg" | sha256sum -c -
          fi
          if [ "$RUNNER_OS" = "Windows" ]; then
            unzip -q gitleaks.pkg gitleaks.exe
            mkdir -p "$RUNNER_TEMP/bin"
            cp gitleaks.exe "$RUNNER_TEMP/bin/gitleaks.exe"
            echo "$RUNNER_TEMP/bin" >> "$GITHUB_PATH"
          else
            tar -xzf gitleaks.pkg gitleaks
            sudo install -m 0755 gitleaks /usr/local/bin/gitleaks
          fi
      - run: python -m pip install pytest
      - shell: bash
        run: PYTHONPATH=src python -m pytest tests -q

  shell-tests:
    # Runs all promoted shell tests under master-ops/scripts/test-*.sh.
    # No Windows leg: these tests require the Unix script execution surface.
    # Probe coverage for the failure path was proven in CI on 2026-09-18.
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: actions/setup-python@v7.0.0
        with:
          python-version: "3.12"
      - name: Run promoted shell tests
        shell: bash
        run: |
          set -u
          shopt -s nullglob
          tmpdir="$(mktemp -d)"
          trap 'rm -rf "$tmpdir"' EXIT
          tests=(master-ops/scripts/test-*.sh)
          if [ "${#tests[@]}" -eq 0 ]; then
            echo "no shell tests matched master-ops/scripts/test-*.sh"
            exit 1
          fi
          passed=0
          failed=0
          skip_lines=0
          while IFS= read -r test_file; do
            name="$(basename "$test_file")"
            out="$tmpdir/$name.out"
            if bash "$test_file" >"$out" 2>&1; then
              code=0
              passed=$((passed + 1))
            else
              code=$?
              failed=$((failed + 1))
            fi
            file_skip="$(grep -c 'SKIP' "$out" || true)"
            skip_lines=$((skip_lines + file_skip))
            echo "$name exit=$code"
            if [ "$code" -ne 0 ]; then
              echo "---- $name output ----"
              cat "$out"
              echo "---- end $name output ----"
            fi
          done < <(printf '%s\n' "${tests[@]}" | LC_ALL=C sort)
          echo "passed $passed failed $failed skip-lines $skip_lines"
          [ "$failed" -eq 0 ]

  redaction:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
      - name: Install gitleaks (pinned, checksum verified)
        shell: bash
        run: |
          case "$(uname -s)-$(uname -m)" in
            Linux-x86_64) f=gitleaks_8.30.1_linux_x64.tar.gz;    sum=551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb ;;
            Darwin-arm64) f=gitleaks_8.30.1_darwin_arm64.tar.gz; sum=b40ab0ae55c505963e365f271a8d3846efbc170aa17f2607f13df610a9aeb6a5 ;;
            *) echo "unmapped runner: $(uname -s)-$(uname -m)"; exit 1 ;;
          esac
          curl -sSfL -o gitleaks.tgz "https://github.com/gitleaks/gitleaks/releases/download/v8.30.1/$f"
          echo "$sum  gitleaks.tgz" | shasum -a 256 -c -
          tar -xzf gitleaks.tgz gitleaks
          sudo install -m 0755 gitleaks /usr/local/bin/gitleaks
      - name: Scan tracked content (committed rules)
        run: ./scripts/redaction-scan.sh
      - name: Inventory (informational — exit 1 is the normal triage state)
        run: ./scripts/redaction-inventory || true
