{
  "_docs": {
    "master_host_runtime": "Agent CLI name the master session runs on (for example claude, codex, or grok).",
    "transcript_globs": "Map of runtime name to a filesystem glob that locates that runtime's session JSONL transcripts for model-identity probes.",
    "product_repositories": "Non-empty array of absolute paths of the product repositories this master serves; the canonical key. The one-entry string form product_repo is still accepted; when both are present, product_repositories wins. Omit both when the workspace has no product repository."
  },
  "master_host_runtime": "claude",
  "transcript_globs": {
    "claude": "~/.claude/projects/-Users-example-workspace/*.jsonl",
    "codex": "~/.codex/sessions/**/*.jsonl",
    "grok": "~/.grok/sessions/**/*.jsonl"
  },
  "product_repositories": ["/absolute/path/to/primary-product-repo"]
}
