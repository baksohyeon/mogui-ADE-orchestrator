# Measured allowlist for Bash commands targeting product-repository roots.
# The current hook-fire-log contains no command field (0 usable command observations),
# so no command is admitted until a measurement run records its actual read-only use.
